
name: build

on:
  push:
  pull_request:
    branches: master

jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v1
      with:
        java-version: '12.x'
    - uses: subosito/flutter-action@v1
      with:
        channel: 'stable' # or: 'beta', 'dev' or 'master'
    - run: flutter pub get
    #IOS
    - run: |
          cd example
          cd ios 
          ruby <(curl https://connect.squareup.com/readersdk-installer) install
          --app-id sq0idp-MgUOLY62bcuvkhDTfYAaOg \
          --repo-password g2g6ie4e5m7xahbwps22d5k6apdpkupa5dckdpgfbtgmfxdhns4q>
          cd ..
          flutter pub get
          flutter build ios --no-codesign
